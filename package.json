{
  "name": "hap-7000-events",
  "version": "1.0.0",
  "description": "HAP's Robot ID Card App — Events Demo. Companion demo for HAP's Learning Lab: The Living Page.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint js/ --ext .js --no-error-on-unmatched-pattern",
    "format": "prettier --write .",
    "test": "vitest",
    "prepare": "husky"
  },
  "lint-staged": {
    "js/**/*.js": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3",
    "prettier": "^3.4.2",
    "vite": "^7.3.1",
    "vitest": "^3.1.1"
  }
}
